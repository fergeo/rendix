doctype html
html
  head
    meta(charset="UTF-8")
    title Listar Cursos
    style.
      body {
        font-family: Arial, sans-serif;
        margin: 30px;
        display: flex;
        flex-direction: column;
        align-items: center;
      }
      h1 {
        color: #333;
        text-align: center;
        margin-bottom: 20px;
      }
      table {
        width: 80%;
        border-collapse: collapse;
        margin-bottom: 30px;
      }
      th, td {
        border: 1px solid #ddd;
        padding: 10px;
        text-align: left;
      }
      th {
        background-color: #f2f2f2;
      }
      .volver-btn {
        padding: 10px 20px;
        font-size: 16px;
        background-color: #4CAF50;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        text-decoration: none;
      }
  body
    h1 Listar Cursos

    table
      thead
        tr
          th Nombre
          th Carrera
          th Día
          th Horario
          th Capacidd
          th Inscriptos
      tbody
        each curso in cursos
          tr
            td #{curso.nombre}
            td #{curso.carrera}
            td #{curso.dia}
            td #{curso.horario}
            td #{curso.capacidad}
            td #{curso.inscriptos}

    a.volver-btn(href="/admin/cursos") Volver
