doctype html
html
    head
        meta(charset="UTF-8")
        title Cursos
        style.
            body {
                font-family: Arial, sans-serif;
                background-color: #f7f7f7;
                padding: 30px;
                text-align: center;
            }
            h1 {
                color: #333;
            }
            .button-container {
                margin-top: 30px;
                display: flex;
                flex-direction: column;
                gap: 15px;
                align-items: center;
            }
            button {
                padding: 10px 20px;
                font-size: 16px;
                cursor: pointer;
                background-color: #4CAF50;
                border: none;
                color: white;
                border-radius: 5px;
                width: 200px;
                transition: background-color 0.3s ease;
            }
            button:hover {
                background-color: #388E3C;
            }
            button.volver {
                background-color: #e53935;
            }
            button.volver:hover {
                background-color: #c62828;
            }
    body
        h1 Cursos

        .button-container
            form(action="/admin/cursos/alta" method="get")
                button(type="submit") Alta de Curso
            form(action="/admin/modificacion-curso" method="get")
                button(type="submit") Modificación de Curso
            form(action="/admin/borrado-curso" method="get")
                button(type="submit") Borrado de Curso
            form(action="/admin/consultar" method="get")
                button(type="submit") Listar Curso                
            form(action="/volver" method="get")
                button.volver(type="submit") Volver
